SELECT employeeID
FROM WorkDone
INNER JOIN advertID ON WorkDone.advertID=TV&Radio.advertID;

SELECT COUNT(clientID)
FROM Campaign 
INNER JOIN Advert ON Magazine.advertID=Advert.advertID
WHERE clientID = "X";

SELECT COUNT(clientID)
FROM Campaign 
INNER JOIN Advert ON Web.advertID=Advert.advertID
WHERE clientID = "X";

SELECT COUNT(clientID)
FROM Campaign 
INNER JOIN Advert ON TVRadio.advertID=Advert.advertID
WHERE clientID = "X";
